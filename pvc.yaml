apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: speedtest-tracker-pvc
spec:
  accessModes:
    - ReadWriteOnce  # Adjust based on your needs (e.g., ReadWriteMany for shared access)
  resources:
    requests:
      storage: 1Gi  # Requested storage size; adjust as needed